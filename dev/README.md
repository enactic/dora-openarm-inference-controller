# Development

## How to release

```bash
git clone git@github.com:enactic/dora-openarm-inference-controller.git
cd dora-openarm-inference-controller
dev/release.sh ${VERSION} # e.g. dev/release.sh 1.0.0
```
